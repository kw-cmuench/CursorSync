<UserControl x:Class="CursorSync.Views.SyncPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Style="{StaticResource PageHeader}">
            <TextBlock Text="Sync" Style="{StaticResource TitleText}"/>
            <TextBlock Style="{StaticResource PageLead}"
                       Text="Push copies this PC into the shared folder. Pull brings shared data here. Two-way keeps the newer file."/>
        </StackPanel>

        <Grid Grid.Row="1" Margin="0,0,12,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="10"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="10"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <RadioButton GroupName="Kind" Style="{StaticResource NavItem}" Margin="0"
                         IsChecked="{Binding SelectedKind, Converter={StaticResource EnumEquals}, ConverterParameter=Push}">
                <RadioButton.Command>
                    <Binding Path="SelectKindCommand"/>
                </RadioButton.Command>
                <RadioButton.CommandParameter>Push</RadioButton.CommandParameter>
                <Border Style="{StaticResource Card}" Padding="16">
                    <StackPanel>
                        <TextBlock Text="Push" FontWeight="SemiBold" FontSize="15"/>
                        <TextBlock Text="This PC → sync folder" Style="{StaticResource MutedText}" Margin="0,6,0,0"/>
                    </StackPanel>
                </Border>
            </RadioButton>

            <RadioButton Grid.Column="2" GroupName="Kind" Style="{StaticResource NavItem}" Margin="0"
                         IsChecked="{Binding SelectedKind, Converter={StaticResource EnumEquals}, ConverterParameter=Pull}"
                         Command="{Binding SelectKindCommand}" CommandParameter="Pull">
                <Border Style="{StaticResource Card}" Padding="16">
                    <StackPanel>
                        <TextBlock Text="Pull" FontWeight="SemiBold" FontSize="15"/>
                        <TextBlock Text="Sync folder → this PC" Style="{StaticResource MutedText}" Margin="0,6,0,0"/>
                    </StackPanel>
                </Border>
            </RadioButton>

            <RadioButton Grid.Column="4" GroupName="Kind" Style="{StaticResource NavItem}" Margin="0"
                         IsChecked="{Binding SelectedKind, Converter={StaticResource EnumEquals}, ConverterParameter=TwoWay}"
                         Command="{Binding SelectKindCommand}" CommandParameter="TwoWay">
                <Border Style="{StaticResource Card}" Padding="16">
                    <StackPanel>
                        <TextBlock Text="Two-way" FontWeight="SemiBold" FontSize="15"/>
                        <TextBlock Text="Newer file wins" Style="{StaticResource MutedText}" Margin="0,6,0,0"/>
                    </StackPanel>
                </Border>
            </RadioButton>
        </Grid>

        <Grid Grid.Row="2" Margin="0,0,12,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Border Style="{StaticResource Card}" Margin="0,0,0,12">
                <StackPanel>
                    <Grid>
                        <TextBlock Text="{Binding HubStatusText}" FontWeight="SemiBold"/>
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                            <Button Content="Choose folder" Style="{StaticResource GhostButton}" Padding="12,7" Command="{Binding BrowseHubCommand}"/>
                            <Button Content="Run sync" Style="{StaticResource PrimaryButton}" Margin="8,0,0,0"
                                    Command="{Binding RunSelectedSyncCommand}"
                                    IsEnabled="{Binding IsSyncing, Converter={StaticResource InverseBool}}"/>
                            <Button Content="Cancel" Style="{StaticResource GhostButton}" Margin="8,0,0,0"
                                    Command="{Binding CancelSyncCommand}"
                                    Visibility="{Binding IsSyncing, Converter={StaticResource BoolToVis}}"/>
                        </StackPanel>
                    </Grid>
                    <TextBlock Text="{Binding HubDetailText}" Style="{StaticResource MutedText}" Margin="0,8,0,12"/>
                    <ProgressBar Value="{Binding ProgressValue}" Maximum="1"
                                 IsIndeterminate="{Binding IsProgressIndeterminate}"
                                 Visibility="{Binding IsSyncing, Converter={StaticResource BoolToVis}}"/>
                    <TextBlock Text="{Binding ProgressText}" Style="{StaticResource CaptionText}" Margin="0,8,0,0"
                               Visibility="{Binding IsSyncing, Converter={StaticResource BoolToVis}}"/>
                </StackPanel>
            </Border>

            <Border Grid.Row="1" Style="{StaticResource Card}" Padding="0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <TextBlock Text="Activity" Style="{StaticResource SectionText}" Margin="18,16,18,8"/>
                    <ListBox Grid.Row="1" ItemsSource="{Binding ActivityLog}" BorderThickness="0" Background="Transparent"
                             ScrollViewer.HorizontalScrollBarVisibility="Disabled" Padding="10,0,10,12">
                        <ListBox.ItemContainerStyle>
                            <Style TargetType="ListBoxItem">
                                <Setter Property="Foreground" Value="{StaticResource MutedBrush}"/>
                                <Setter Property="FontFamily" Value="Cascadia Mono, Consolas"/>
                                <Setter Property="FontSize" Value="12"/>
                                <Setter Property="Padding" Value="8,3"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="ListBoxItem">
                                            <ContentPresenter/>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </ListBox.ItemContainerStyle>
                    </ListBox>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</UserControl>
